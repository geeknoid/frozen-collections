[package]
name = "no-std-test"
version = "0.0.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true
publish = false

[dependencies]
frozen-collections = { path = "../frozen-collections", default-features = false, features = ["macros"] }

[lints]
workspace = true
